const ConnectGraphqlModel = require('./connect_graphql.model');
const typeDef = require('./connect_graphql.typedef');
const resolver = require('./connect_graphql.resolvers');

module.exports = {
  ConnectGraphqlModel,
  typeDef,
  resolver,
};
